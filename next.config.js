/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['image.wanted.co.kr', 'avatars.githubusercontent.com'],
  },
};

module.exports = nextConfig;
